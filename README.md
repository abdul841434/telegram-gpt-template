# 🤖 Empathy AI Bot (Эдя)

Интеллектуальный Telegram-бот с искусственной эмпатией для эмоциональной поддержки и активного слушания. Использует языковые модели через OpenRouter API для ведения дружелюбных бесед, запоминает контекст и отправляет напоминания для поддержания диалога.

## 📋 Описание

**Эдя** — это чатбот, который:
- 💬 Ведет естественные диалоги с пользователями
- 🧠 Запоминает контекст предыдущих сообщений
- ⏰ Отправляет умные напоминания для продолжения общения
- 🎯 Активно интересуется жизнью пользователя
- 📊 Хранит историю диалогов в локальной базе данных
- 🔧 Поддерживает администраторские команды для управления

## ✨ Возможности

### Для пользователей:
- Естественное общение с AI-ассистентом
- Сохранение контекста беседы (до MAX_CONTEXT сообщений)
- Автоматические напоминания о продолжении диалога
- Возможность сбросить историю диалога
- Поддержка Markdown форматирования в ответах

### Для администраторов:
- Отправка сообщений конкретному пользователю
- Массовая рассылка всем пользователям
- Отслеживание активности пользователей
- Отладочный режим с пересылкой сообщений

## 🛠 Технологии

- **Python 3.14+**
- **aiogram 3.20** - асинхронный фреймворк для Telegram Bot API
- **aiosqlite** - асинхронная работа с SQLite
- **aiohttp** - асинхронные HTTP-запросы к OpenRouter API
- **OpenRouter API** - доступ к различным LLM моделям
- **telegramify-markdown** - конвертация Markdown для Telegram

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd empathy-ai-bot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Активация на macOS/Linux:
source venv/bin/activate

# Активация на Windows:
venv\Scripts\activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте файл `.env.example` в `.env` и заполните необходимые значения:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл:

```bash
nano .env  # или используйте любой текстовый редактор
```

#### Обязательные параметры:

- `TG_TOKEN` — токен вашего Telegram бота (получите у [@BotFather](https://t.me/BotFather))
- `LLM_TOKEN` — API ключ от [OpenRouter](https://openrouter.ai/)
- `MODEL` — модель LLM для использования
- `DEBUG_CHAT` — ваш Telegram chat ID (получите через [@userinfobot](https://t.me/userinfobot))

#### Рекомендуемые модели:

- `google/gemini-2.0-flash-exp:free` — бесплатная, быстрая
- `openai/gpt-4o-mini` — платная, качественная
- `anthropic/claude-3.5-sonnet` — платная, очень качественная

Полный список моделей: https://openrouter.ai/models

## 🚀 Запуск

```bash
python main.py
```

Бот автоматически:
- Создаст базу данных (если её нет)
- Запустит Telegram polling
- Начнет проверять напоминания каждые 30 секунд

### Остановка бота

Для корректной остановки нажмите **Ctrl-C** в терминале.

Если бот завис и не реагирует на Ctrl-C, используйте скрипт:

```bash
./kill_bot.sh
```

Скрипт покажет найденные процессы и запросит подтверждение перед остановкой.

## 📝 Команды бота

### Пользовательские команды:

- `/start` — приветствие и информация о боте
- `/help` — список доступных команд
- `/forget` — сбросить историю диалога
- `/reminder` — отключить напоминания

### Команды администратора:

- `/dispatch` — отправить сообщение конкретному пользователю
- `/dispatch_all` — отправить сообщение всем пользователям

## 🗂 Структура проекта

```
empathy-ai-bot/
├── main.py              # Основная логика бота
├── openrouters.py       # Интеграция с OpenRouter API
├── user_db.py          # Работа с базой данных пользователей
├── prompts.json        # Системные промпты для LLM
├── messages.json       # Шаблоны сообщений бота
├── requirements.txt    # Зависимости Python
├── kill_bot.sh         # Скрипт для остановки бота
├── .env               # Переменные окружения (не в git)
├── .env.example       # Пример переменных окружения
├── .gitignore         # Игнорируемые файлы
└── README.md          # Документация
```

## 🗄 База данных

Бот использует SQLite для хранения данных пользователей:

### Структура таблицы `users`:

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | Telegram chat ID (PRIMARY KEY) |
| `name` | TEXT | Username пользователя |
| `prompt` | JSON | История диалога |
| `remind_of_yourself` | TEXT | Дата/время следующего напоминания |
| `sub_lvl` | INTEGER | Уровень подписки |
| `sub_id` | TEXT | ID подписки |
| `sub_period` | INTEGER | Период подписки |
| `is_admin` | INTEGER | Флаг администратора |

## ⚙️ Конфигурация

### Настройка промптов

Редактируйте `prompts.json` для изменения поведения бота:

- `DEFAULT_PROMPT` — базовый системный промпт для диалога
- `REMINDER_PROMPT` — промпт для генерации напоминаний

### Настройка сообщений

Редактируйте `messages.json` для изменения текстов сообщений бота.

### Настройка напоминаний

В `.env` файле:

```env
DELAYED_REMINDERS_HOURS=2      # Напоминание через 2 часа
DELAYED_REMINDERS_MINUTES=0    # + 0 минут
FROM_TIME=9                     # Не раньше 9:00
TO_TIME=23                      # Не позже 23:00
TIMEZONE_OFFSET=3               # Часовой пояс UTC+3
```

## 📊 Логирование

Бот ведет логи в двух режимах:

- **Console** — уровень INFO (общая информация)
- **debug.log** — уровень DEBUG (детальные логи)

Файл `debug.log` автоматически ротируется (до 5 файлов по 1 МБ).

## 🔧 Режим отладки

Включите отладку в `.env`:

```env
DEBUG=1
DEBUG_CHAT=your_chat_id
```

В режиме отладки все сообщения будут пересылаться в указанный чат.

## 🛡 Безопасность

⚠️ **Важно:**

- Никогда не коммитьте `.env` файл в репозиторий
- Храните токены в секрете
- Используйте разные токены для dev/prod окружений
- Регулярно обновляйте зависимости

## 📈 Масштабирование

Для production использования рекомендуется:

- Использовать PostgreSQL вместо SQLite
- Добавить Redis для кэширования
- Настроить webhook вместо polling
- Добавить мониторинг (Prometheus, Grafana)
- Использовать Docker для деплоя

## 🤝 Разработка

### Требования:
- Python 3.14.3+
- Все зависимости из `requirements.txt`

### Запуск в режиме разработки:

```bash
# Установка зависимостей для разработки
pip install -r requirements.txt

# Включение отладки
DEBUG=1 python main.py
```

## ❗ Решение проблем

### Бот не отвечает на сообщения

1. Проверьте правильность `TG_TOKEN`
2. Убедитесь, что бот запущен
3. Проверьте логи в `debug.log`

### Ошибки LLM

1. Проверьте `LLM_TOKEN` и баланс на OpenRouter
2. Проверьте доступность модели `MODEL`
3. Проверьте логи для деталей ошибки

### Проблемы с базой данных

1. Убедитесь, что есть права на запись
2. Проверьте `DATABASE_NAME` и `TABLE_NAME`
3. Удалите `.db` файл и перезапустите бота

## 🤖 Демо-версия

**Попробуйте бота прямо сейчас:** [@kindness365bot](https://t.me/kindness365bot)
