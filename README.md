# 🤖 Empathy AI Bot (Эдя)

Интеллектуальный Telegram-бот с искусственной эмпатией для эмоциональной поддержки и активного слушания. Использует языковые модели через OpenRouter API для ведения дружелюбных бесед, запоминает контекст и отправляет напоминания для поддержания диалога.

## 📋 Описание

**Эдя** — это чатбот, который:
- 💬 Ведет естественные диалоги с пользователями
- 🧠 Запоминает контекст предыдущих сообщений
- ⏰ Отправляет умные напоминания для продолжения общения
- 🎯 Активно интересуется жизнью пользователя
- 📊 Хранит историю диалогов в локальной базе данных
- 👥 Работает в групповых чатах (с упоминанием @username или ответом на сообщение)
- 🔒 Поддерживает обязательную подписку на каналы-спонсоры
- 🔧 Поддерживает администраторские команды для управления

## ✨ Возможности

### Для пользователей:
- Естественное общение с AI-ассистентом
- Сохранение истории беседы (до MAX_STORAGE сообщений в БД)
- Контекст для модели (последние MAX_CONTEXT сообщений)
- Автоматические напоминания о продолжении диалога
- Возможность сбросить историю диалога
- Поддержка Markdown форматирования в ответах
- **Работа в групповых чатах** - добавьте бота в чат с друзьями и общайтесь вместе!
- **Умная буферизация** - отправляйте несколько сообщений подряд, бот ответит на все сразу!

### Групповые чаты:
- 👥 Бот работает в групповых чатах и супергруппах
- 🔔 При добавлении в чат отправляется приветственное сообщение
- ✅ Для использования необходимо подтвердить подписку на каналы (один раз на чат)
- 💬 Бот отвечает при упоминании (@bot_username) или ответе на его сообщение
- 🗑️ При удалении бота все данные чата автоматически удаляются

### Система подписки:
- 🔒 **Обязательная подписка на каналы-спонсоры** для использования бота
- 📢 Настраивается список обязательных каналов через переменную `REQUIRED_CHANNELS`
- ✅ Проверка подписки при первом использовании
- 🔄 Автоматическая фоновая проверка каждые 30 минут
- 🚫 При отписке доступ к боту блокируется
- 👥 Для групповых чатов: достаточно подписки одного участника

### Для администраторов:
- Отправка сообщений конкретному пользователю
- Массовая рассылка всем пользователям
- Отслеживание активности пользователей
- Отладочный режим с пересылкой сообщений с USER ID
- Просмотр статистики активности по часам и дням недели (графики)

## 🛠 Технологии

- **Python 3.14+**
- **aiogram 3.20** - асинхронный фреймворк для Telegram Bot API
- **aiosqlite** - асинхронная работа с SQLite
- **aiohttp** - асинхронные HTTP-запросы к OpenRouter API
- **OpenRouter API** - доступ к различным LLM моделям
- **telegramify-markdown** - конвертация Markdown для Telegram
- **matplotlib** - генерация графиков статистики

## 📦 Быстрый старт

### 3 команды и готово! 🚀

```bash
git clone <repository-url> && cd empathy-ai-bot
cp .env.example .env && nano .env  # Заполните 4 токена
docker-compose up -d
```

Вот и всё! Бот запущен. 

### Нужны токены?

- `TG_TOKEN` → [@BotFather](https://t.me/BotFather)
- `LLM_TOKEN` → [OpenRouter](https://openrouter.ai/)
- `MODEL` → `google/gemini-2.0-flash-exp:free`
- `ADMIN_CHAT` → [@userinfobot](https://t.me/userinfobot)

### Без Docker?

```bash
python -m venv venv && source venv/bin/activate
pip install -r requirements.txt
cp .env.example .env && nano .env
python main.py
```

📖 **[Быстрый старт →](docs/quickstart.md)** | **[Деплой →](docs/deployment.md)** | **[CI/CD →](docs/ci-cd.md)** | **[Буферизация →](docs/message-buffering.md)**

## 📝 Команды бота

### Пользовательские команды:

- `/start` — приветствие и информация о боте
- `/help` — список доступных команд
- `/forget` — сбросить историю диалога
- `/mute` — отключить напоминания

### Команды администратора:

- `/dispatch` — отправить сообщение конкретному пользователю
- `/dispatch_all` — отправить сообщение всем пользователям
- `/stats` — просмотр статистики активности пользователя (в ответ на сообщение с USER ID) или всех пользователей (без ответа)

## 🏗 Архитектура

Проект имеет модульную структуру с разделением ответственности:

### Основные модули:

- **`main.py`** — точка входа, запуск приложения
- **`config.py`** — конфигурация, загрузка переменных окружения, настройка логирования
- **`bot_instance.py`** — инициализация бота и диспетчера
- **`database.py`** — модели данных и работа с БД

### Вспомогательные модули:

- **`filters.py`** — пользовательские фильтры для роутинга сообщений
- **`states.py`** — FSM состояния для многошаговых диалогов
- **`utils.py`** — общие утилиты (typing indicator, debug forwarding)

### Обработчики (handlers/):

- **`user_handlers.py`** — команды пользователей (/start, /help, /forget, etc.)
- **`admin_handlers.py`** — команды администраторов (/dispatch, /dispatch_all)
- **`message_handlers.py`** — обработка текстовых сообщений

### Сервисы (services/):

- **`llm_client.py`** — клиент для работы с OpenRouter API
- **`llm_service.py`** — логика взаимодействия с LLM, обработка запросов
- **`reminder_service.py`** — логика напоминаний, фоновая задача

## 🗂 Структура проекта

```
empathy-ai-bot/
├── .github/
│   └── workflows/
│       └── deploy.yml         # GitHub Actions CI/CD
├── config/                     # Конфигурационные файлы
│   ├── prompts.json           # Системные промпты для LLM
│   └── messages.json          # Шаблоны сообщений
├── docs/                       # Документация
│   ├── quickstart.md          # Быстрый старт за 3 минуты
│   ├── deployment.md          # Полное руководство по развертыванию
│   ├── ci-cd.md              # Настройка автодеплоя
│   ├── logging.md            # Настройка и уровни логирования
│   └── migrations.md         # Система миграций БД
├── handlers/                   # Обработчики команд и сообщений
│   ├── __init__.py
│   ├── user_handlers.py       # Пользовательские команды
│   ├── admin_handlers.py      # Администраторские команды
│   └── message_handlers.py    # Обработка текстовых сообщений
├── services/                   # Бизнес-логика
│   ├── __init__.py
│   ├── llm_client.py          # Клиент OpenRouter API
│   ├── llm_service.py         # Работа с LLM
│   └── reminder_service.py    # Логика напоминаний
├── migrations/                 # Миграции базы данных
│   ├── __init__.py
│   ├── migration_manager.py   # Менеджер миграций
│   └── migration_001_*.py     # Файлы миграций
├── data/                       # База данных (создается автоматически)
├── main.py                     # Точка входа приложения
├── config.py                   # Конфигурация и настройки
├── bot_instance.py             # Инициализация бота
├── filters.py                  # Пользовательские фильтры
├── states.py                   # FSM состояния
├── utils.py                    # Вспомогательные функции
├── database.py                 # Работа с базой данных
├── requirements.txt           # Зависимости Python
├── Dockerfile                 # Docker образ
├── docker-compose.yml         # Docker Compose конфигурация
├── .dockerignore              # Исключения для Docker
├── README.md                  # Основная документация
├── .env.example               # Пример переменных окружения
└── .gitignore                 # Игнорируемые файлы
```

## 🗄 База данных

Бот использует SQLite для хранения данных пользователей:

### Структура таблицы `users`:

| Поле | Тип | Описание |
|------|-----|----------|
| `id` | INTEGER | Telegram chat ID (PRIMARY KEY) |
| `name` | TEXT | Username пользователя |
| `prompt` | JSON | История диалога (массив сообщений) |
| `remind_of_yourself` | TEXT | Дата/время следующего напоминания |
| `sub_lvl` | INTEGER | Уровень подписки |
| `sub_id` | TEXT | ID подписки |
| `sub_period` | INTEGER | Период подписки |
| `is_admin` | INTEGER | Флаг администратора |

### Структура сообщения в истории:

Каждое сообщение в поле `prompt` имеет следующую структуру:

```json
{
  "role": "user",
  "content": "Текст сообщения",
  "timestamp": "2025-11-05 15:30:45"
}
```

| Поле | Тип | Описание |
|------|-----|----------|
| `role` | string | Роль: `"user"` или `"assistant"` |
| `content` | string | Текст сообщения |
| `timestamp` | string\|null | Время отправки с учетом TIMEZONE_OFFSET (или `null` для старых сообщений) |

**Примечание:** Старые сообщения, созданные до миграции, имеют `timestamp: null`.

## ⚙️ Конфигурация

### Настройка поведения бота

- **`config/prompts.json`** — системные промпты для LLM (основной диалог и напоминания)
- **`config/messages.json`** — текстовые шаблоны сообщений бота

### Основные параметры в `.env`

```env
# Напоминания
DELAYED_REMINDERS_HOURS=2      # Напоминание через N часов
DELAYED_REMINDERS_MINUTES=0    # + дополнительные минуты
FROM_TIME=9                     # Начало временного окна (час)
TO_TIME=23                      # Конец временного окна (час)
TIMEZONE_OFFSET=3               # Часовой пояс (UTC+3 для МСК)

# Контекст и БД
MAX_CONTEXT=20                  # Количество сообщений, передаваемых в контекст модели
MAX_STORAGE=100                 # Количество сообщений, хранимых в базе данных
DATABASE_NAME=users.db          # Имя файла базы данных

# Подписка на каналы (опционально)
REQUIRED_CHANNELS=@channel1,@channel2   # Список обязательных каналов через запятую
                                         # Оставьте пустым, чтобы отключить систему подписки
```

📖 **[Подробнее о конфигурации →](docs/deployment.md#настройка-переменных-окружения)**

## 📊 Логирование

Бот использует гибкую трехуровневую систему логирования:
- **Консоль** - только ошибки (ERROR+) и startup сообщения
- **Файл** (`debug.log`) - настраиваемый уровень детализации
- **Telegram** (ADMIN_CHAT) - опциональные логи в чат для мониторинга

### Уровни логирования:

| Уровень | Что логируется |
|---------|----------------|
| **DISABLED** | Логи отключены |
| **CRITICAL** | Только критические ошибки |
| **ERROR** | + Ошибки |
| **WARNING** | + Предупреждения |
| **MESSAGES** | Пересылка сообщений пользователей (без текстовых логов) |
| **INFO** | + Текстовые логи: сообщения + ответы бота |
| **DEBUG** | + Системные промпты, сырой вывод LLM |
| **FULL** | + Полные промпты со всей историей |

### Настройка в `.env`:

```env
# Логирование в файл debug.log
FILE_LOG_LEVEL=INFO

# Логирование в Telegram чат (опционально)
TELEGRAM_LOG_LEVEL=DISABLED

# ID чата для debug логов
ADMIN_CHAT=123456789
```

### Примеры конфигураций:

**Production (минимум):**
```env
FILE_LOG_LEVEL=WARNING
TELEGRAM_LOG_LEVEL=CRITICAL
```

**Production (стандарт):**
```env
FILE_LOG_LEVEL=INFO
TELEGRAM_LOG_LEVEL=ERROR
```

**Мониторинг сообщений пользователей:**
```env
FILE_LOG_LEVEL=INFO
TELEGRAM_LOG_LEVEL=MESSAGES  # Пересылка USER сообщений (без текстовых логов INFO)
```
> Пересылка сообщений работает только на уровнях ≤ MESSAGES (MESSAGES, INFO, DEBUG, FULL)

**Разработка:**
```env
FILE_LOG_LEVEL=DEBUG
TELEGRAM_LOG_LEVEL=INFO
```

**Отладка промптов:**
```env
FILE_LOG_LEVEL=FULL  # полные промпты с историей
TELEGRAM_LOG_LEVEL=DISABLED
```

### Просмотр логов:

```bash
# Локально
tail -f debug.log

# Docker (startup сообщения и ошибки)
docker-compose logs -f

# Только ошибки
grep "ERROR" debug.log

# Промпты конкретного пользователя
grep "PROMPT.*123456" debug.log
```

### Telegram мониторинг:

Включите `TELEGRAM_LOG_LEVEL` для получения логов в Telegram чат:
- Удобно для production - не нужен SSH доступ
- Мгновенные уведомления об ошибках
- **MESSAGES** - пересылка сообщений пользователей (без текстовых логов)
- **INFO/DEBUG/FULL** - текстовые логи + пересылка сообщений
- **WARNING/ERROR/CRITICAL** - только текстовые логи (пересылка отключена)
- Рекомендуется для production: `ERROR` или `WARNING`

📖 **[Полная документация по логированию →](docs/logging.md)**

## 🛡 Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` файл
- Храните токены в секрете
- Используйте разные токены для dev/prod
- Регулярно обновляйте зависимости

## ❗ Решение проблем

Основные проблемы и их решения:

| Проблема | Решение |
|----------|---------|
| Бот не отвечает | Проверьте `TG_TOKEN`, статус и логи |
| Ошибки LLM | Проверьте `LLM_TOKEN`, баланс и модель |
| Ошибки БД | Проверьте права доступа, пересоздайте БД |
| Бот не останавливается | Нажмите `Ctrl-C` в терминале |

📖 **[Полное руководство по решению проблем →](docs/deployment.md#решение-проблем)**

## 🚀 CI/CD и автоматический деплой

Настроен автоматический деплой на Yandex Cloud через GitHub Actions.

### Что происходит при push в main:

1. ✅ Автоматическое тестирование
2. 🏗️ Сборка Docker образа
3. 📦 Загрузка в Container Registry
4. 🚀 Деплой на сервер
5. ✅ Проверка работоспособности

**Время деплоя:** 2-3 минуты

📖 **[Настроить автоматический деплой →](docs/ci-cd.md)**

## 🤖 Демо-версия

**Попробуйте бота прямо сейчас:** [@kindness365bot](https://t.me/kindness365bot)
