# Система логирования

Бот использует гибкую систему логирования с тремя выходами:
- **Консоль** (stdout) - только для ошибок (ERROR+) и startup сообщений
- **Файл** (`debug.log`) - настраиваемый уровень детализации
- **Telegram** (DEBUG_CHAT) - настраиваемый уровень детализации

## Уровни логирования

Система поддерживает следующие уровни (от менее детального к более детальному):

| Уровень | Значение | Что логируется |
|---------|----------|----------------|
| **DISABLED** | - | Логи отключены полностью |
| **CRITICAL** | 50 | Только критические ошибки |
| **ERROR** | 40 | + Ошибки |
| **WARNING** | 30 | + Предупреждения (блокировки пользователей и т.д.) |
| **INFO** | 20 | + Сообщения пользователей, ответы бота, статистика промптов |
| **DEBUG** | 10 | + Системные промпты (без истории), сырой вывод LLM |
| **FULL** | 5 | + Полные промпты со всей историей диалога |

## Настройка в `.env`

```bash
# Уровень логирования в файл debug.log
# Варианты: DISABLED, CRITICAL, ERROR, WARNING, INFO, DEBUG, FULL
FILE_LOG_LEVEL=INFO

# Уровень логирования в Telegram чат
# Варианты: DISABLED, CRITICAL, ERROR, WARNING, INFO, DEBUG, FULL
TELEGRAM_LOG_LEVEL=DISABLED

# ID чата для debug логов
DEBUG_CHAT=123456789
```

## Рекомендуемые конфигурации

### 1. Production - минимальное логирование
```bash
FILE_LOG_LEVEL=WARNING
TELEGRAM_LOG_LEVEL=CRITICAL
```
**Результат:** В файле только предупреждения и ошибки, в Telegram только критические ошибки

### 2. Production - стандартное логирование
```bash
FILE_LOG_LEVEL=INFO
TELEGRAM_LOG_LEVEL=ERROR
```
**Результат:** В файле все важные события, в Telegram только ошибки

### 3. Разработка - подробное логирование
```bash
FILE_LOG_LEVEL=DEBUG
TELEGRAM_LOG_LEVEL=INFO
```
**Результат:** В файле всё включая системные промпты, в Telegram основные события

### 4. Отладка промптов - максимальное логирование
```bash
FILE_LOG_LEVEL=FULL
TELEGRAM_LOG_LEVEL=DISABLED
```
**Результат:** В файле полные промпты с историей, Telegram отключен

### 5. Без логов (не рекомендуется)
```bash
FILE_LOG_LEVEL=DISABLED
TELEGRAM_LOG_LEVEL=DISABLED
```
**Результат:** Логи полностью отключены (кроме ERROR в консоль)

## Что пишется на каждом уровне

### INFO уровень
```
2025-11-05 16:00:00 - INFO - Logger initialized: FILE=INFO, TELEGRAM=DISABLED
2025-11-05 16:00:02 - INFO - USER241248104TOLLM:Привет!
2025-11-05 16:00:03 - INFO - PROMPT_STATS_MESSAGE241248104: messages={user: 1, assistant: 0, system: 1}, total_chars=450
2025-11-05 16:00:05 - INFO - LLM241248104 - Привет! Как дела?
2025-11-05 16:00:10 - INFO - Telegram logging handler enabled
```

### DEBUG уровень (+ всё из INFO)
```
2025-11-05 16:00:03 - DEBUG - PROMPT_SYSTEM_MESSAGE241248104: [{"role": "system", "content": "..."}]
2025-11-05 16:00:05 - DEBUG - LLM_RAWOUTPUT241248104:Привет! Как дела?
2025-11-05 16:00:10 - DEBUG - USER241248104 имя обновлено: Никита
```

### FULL уровень (+ всё из DEBUG)
```
2025-11-05 16:00:03 - FULL - PROMPT_FULL_MESSAGE241248104: [
  {
    "role": "user",
    "content": "Привет!"
  },
  {
    "role": "system",
    "content": "1. Ты общительный ассистент..."
  }
]
```

### WARNING уровень
```
2025-11-05 16:00:15 - WARNING - USER241248104 заблокировал чатбота
```

### ERROR уровень
```
2025-11-05 16:00:20 - ERROR - LLM241248104 - Критическая ошибка: Connection timeout
Traceback (most recent call last):
  ...
```

## Консольный вывод

В консоль (stdout) выводятся только:
1. **Startup сообщения** при запуске бота:
```
==================================================
🤖 БОТ ЗАПУЩЕН
==================================================
Database: Бд подгружена успешно
Debug chat: 123456789
Нажмите Ctrl-C для остановки бота
==================================================
```

2. **ERROR и CRITICAL** логи с полным traceback (для отладки)

3. **Shutdown сообщения** при остановке бота:
```
🛑 Получен сигнал остановки
Останавливаем бота...
Цикл напоминаний остановлен
✅ Бот остановлен
```

## Telegram логирование

Логи в Telegram отправляются в чат `DEBUG_CHAT` в формате:
```
INFO: USER241248104TOLLM:Привет!
ERROR: LLM241248104 - Критическая ошибка: Connection timeout
WARNING: USER241248104 заблокировал чатбота
```

**Преимущества:**
- Мониторинг в реальном времени
- Уведомления о проблемах
- Не нужен доступ к серверу

**Рекомендации:**
- Для production используйте `ERROR` или `CRITICAL`
- Для тестирования можно использовать `INFO`
- Не используйте `DEBUG` или `FULL` - слишком много сообщений

## Файловое логирование

Логи сохраняются в файл `debug.log` с автоматической ротацией:
- Максимальный размер файла: **1 MB**
- Количество backup файлов: **5**
- Кодировка: **UTF-8**

Файлы:
- `debug.log` - текущий лог
- `debug.log.1` - предыдущий (после ротации)
- `debug.log.2` ... `debug.log.5` - еще более старые

## Полезные команды

### Просмотр логов в реальном времени
```bash
tail -f debug.log
```

### Просмотр только ошибок
```bash
grep "ERROR\|CRITICAL" debug.log
```

### Просмотр логов конкретного пользователя
```bash
grep "241248104" debug.log
```

### Просмотр промптов
```bash
# Статистика
grep "PROMPT_STATS" debug.log

# Системные промпты
grep "PROMPT_SYSTEM" debug.log

# Полные промпты (если FILE_LOG_LEVEL=FULL)
grep "PROMPT_FULL" debug.log
```

### Просмотр только сегодняшних логов
```bash
grep "$(date +%Y-%m-%d)" debug.log
```

### Подсчет ошибок
```bash
grep -c "ERROR" debug.log
```

### Docker logs
```bash
# Через docker-compose
docker-compose logs
docker-compose logs -f  # в реальном времени
docker-compose logs --tail=100  # последние 100 строк

# Или напрямую по имени контейнера
docker logs empathy-ai-bot
docker logs -f empathy-ai-bot  # в реальном времени
docker logs empathy-ai-bot --tail=100  # последние 100 строк

# Проверить статус контейнера
docker ps
```

## Устранение неполадок

### Логи не пишутся в файл

**Проблема:** После изменения `FILE_LOG_LEVEL` логи не появляются.

**Решение:**
1. Перезапустите бота
2. Проверьте первую строку в `debug.log`:
```bash
grep "Logger initialized" debug.log | tail -1
```
3. Должна быть строка: `Logger initialized: FILE=INFO, TELEGRAM=DISABLED`

### Логи не приходят в Telegram

**Проблема:** `TELEGRAM_LOG_LEVEL` установлен, но логи не приходят в чат.

**Решение:**
1. Проверьте что `TELEGRAM_LOG_LEVEL` не равен `DISABLED`
2. Проверьте что `DEBUG_CHAT` правильный (узнать ID: @userinfobot)
3. Проверьте что бот не заблокирован в чате
4. Перезапустите бота
5. Проверьте логи:
```bash
grep "Telegram logging handler enabled" debug.log
```

### Слишком много логов

**Проблема:** Файл `debug.log` быстро растет.

**Решение:**
- Уменьшите `FILE_LOG_LEVEL` (например, с `DEBUG` на `INFO`)
- Или увеличьте размер файла в `config.py`:
```python
maxBytes=5 * 1024 * 1024  # 5MB вместо 1MB
```

### PROMPT_FULL не появляется

**Проблема:** В логах есть `PROMPT_SYSTEM` и `PROMPT_STATS`, но нет `PROMPT_FULL`.

**Причина:** `FILE_LOG_LEVEL` не установлен в `FULL`.

**Решение:**
```bash
# В .env
FILE_LOG_LEVEL=FULL

# Перезапустите бота
```

## Безопасность

⚠️ **Важно:**

1. **Защитите `debug.log`:**
   - Файл содержит полную историю диалогов
   - Не коммитьте его в git (добавлено в `.gitignore`)
   - Ограничьте доступ на сервере

2. **DEBUG_CHAT:**
   - Используйте приватный чат
   - Не используйте публичные группы
   - Регулярно проверяйте кто имеет доступ

3. **Уровень FULL:**
   - Не используйте в production постоянно
   - Включайте только для отладки
   - Логи содержат ВСЮ историю диалогов

4. **Ротация логов:**
   - Старые логи автоматически удаляются (> 5 backup файлов)
   - При необходимости архивируйте важные логи

## Примеры использования

### Мониторинг в production

```bash
# .env
FILE_LOG_LEVEL=INFO
TELEGRAM_LOG_LEVEL=ERROR
```

Вы получите:
- ✅ В файле - все события для аналитики
- ✅ В Telegram - только ошибки для быстрого реагирования
- ✅ В консоли - traceback при критических ошибках

### Отладка проблем с LLM

```bash
# .env
FILE_LOG_LEVEL=FULL
TELEGRAM_LOG_LEVEL=DISABLED
```

Затем:
```bash
# Воспроизведите проблему
# Найдите полный промпт
grep "PROMPT_FULL_MESSAGE123456" debug.log | tail -1

# Проверьте что отправляется в LLM
```

### Разработка новых функций

```bash
# .env
FILE_LOG_LEVEL=DEBUG
TELEGRAM_LOG_LEVEL=INFO
```

Вы получите:
- ✅ Подробные логи в файле для отладки
- ✅ Основные события в Telegram для удобства
- ✅ Быстрый доступ к логам без SSH

## Миграция со старой системы

Если у вас была старая версия с `DEBUG=1/0`:

### Было:
```bash
DEBUG=1
DEBUG_CHAT=123456789
LOG_LEVEL=INFO
CONSOLE_LOG_LEVEL=INFO
FILE_LOG_LEVEL=DEBUG
```

### Стало:
```bash
# DEBUG больше не нужен, удалите его
DEBUG_CHAT=123456789

# Новые параметры
FILE_LOG_LEVEL=DEBUG
TELEGRAM_LOG_LEVEL=INFO  # или DISABLED
```

**Изменения:**
- ❌ Убрана переменная `DEBUG`
- ❌ Убраны переменные `LOG_LEVEL` и `CONSOLE_LOG_LEVEL`
- ✅ Добавлена переменная `TELEGRAM_LOG_LEVEL`
- ✅ Консоль теперь всегда показывает только ERROR+
- ✅ Telegram логирование опционально
