# Поддержка групповых чатов

## Обзор изменений

Бот поддерживает работу в групповых чатах с новой моделью верификации:

### Ключевые изменения:

1. **Раздельная логика для чатов и ЛС**
   - В личных сообщениях: бот отвечает на все сообщения
   - В групповых чатах: бот отвечает только когда его упоминают (`@bot_username`) или отвечают на его сообщение

2. **Новая модель верификации для чатов**
   - Для использования бота в чате достаточно, чтобы **хотя бы один участник** подписался на обязательные каналы
   - Информация о верификации чата хранится в отдельной таблице `chat_verifications`
   - После верификации все участники чата могут пользоваться ботом

## Архитектура

### Таблица `chat_verifications`

```sql
CREATE TABLE chat_verifications (
    chat_id INTEGER PRIMARY KEY,           -- ID чата (отрицательный)
    verified_by_user_id INTEGER NOT NULL,  -- кто подтвердил подписку
    verified_at TEXT NOT NULL,             -- когда подтвердил
    user_name TEXT                         -- имя верификатора
)
```

### Новый класс `ChatVerification`

Добавлен класс `ChatVerification` в `database.py` с методами:
- `get_from_db()` - загрузка информации о верификации
- `save_to_db()` - сохранение/обновление верификации
- `delete_from_db()` - удаление верификации
- `is_chat_verified(chat_id)` - проверка, верифицирован ли чат

## Логика работы

### Для личных чатов (как раньше):
1. Пользователь отправляет любое сообщение боту
2. Middleware проверяет `subscription_verified` пользователя
3. Если не подписан (значение = 0) → просит подписаться
4. После нажатия "Я подписался" → проверка подписки → обновление `subscription_verified = 1`
5. Подтверждение отправляется в **личные сообщения**

### Для групповых чатов (новое):
1. Участник отправляет сообщение с упоминанием бота
2. Middleware проверяет наличие записи в `chat_verifications` для этого чата
3. Если чат не верифицирован → просит подписаться (сообщение в чат)
4. Любой участник нажимает "Я подписался"
5. Проверяется подписка этого участника
6. Если подписан → создается запись в `chat_verifications`
7. Подтверждение отправляется в **чат** (не в ЛС!)
8. Теперь все участники чата могут пользоваться ботом

### Проверка упоминаний в чатах

Бот отвечает в групповом чате, если:
- Сообщение является ответом на сообщение бота
- Бот упомянут через `@bot_username` в тексте
- Бот упомянут через entities (mention/text_mention)
- Бот упомянут в caption (для фото/видео)

## Измененные файлы

### Core файлы:
1. **`database.py`** - добавлен класс `ChatVerification`
2. **`utils.py`** - добавлены функции:
   - `is_private_chat()` - проверка типа чата
   - `should_respond_in_chat()` - проверка упоминаний бота
3. **`middlewares.py`** - обновлен `SubscriptionMiddleware` для разделения логики чатов/ЛС

### Handlers:
4. **`handlers/subscription_handlers.py`** - обновлен для работы с чатами:
   - `send_subscription_request()` - различает чаты/ЛС
   - `process_subscription_check()` - сохраняет верификацию чата
5. **`handlers/message_handlers.py`** - добавлена проверка упоминаний для всех типов сообщений

### Конфигурация:
6. **`config/messages.json`** - добавлены новые сообщения:
   - `msg_subscription_required_chat` - для чатов
   - `msg_subscription_verified_chat` - подтверждение в чате
   - `msg_chat_not_mentioned` - когда бота не упомянули

### База данных:
7. **`database.py`** - функция `check_db()` создает таблицу `chat_verifications` при инициализации

## Примеры использования

### В личных сообщениях:
```
Пользователь: Привет!
Бот: [если не подписан] 🔔 Для использования бота необходимо подписаться...
Пользователь: [нажимает "Я подписался"]
Бот: [в ЛС] ✅ Отлично! Подписка подтверждена...
```

### В групповом чате:
```
Участник1: @bot расскажи анекдот
Бот: [если чат не верифицирован] 🔔 Для использования бота в этом чате необходимо...
Участник2: [нажимает "Я подписался"]
Бот: [в чат] ✅ Отлично! Подписка подтверждена пользователем Участник2. Теперь все участники чата могут пользоваться ботом!
Участник1: @bot расскажи анекдот
Бот: [отвечает анекдотом]
```

## Проверка работы

Проверить структуру таблицы:
```bash
docker-compose exec telegram-gpt python -c "
import asyncio
import aiosqlite

async def check():
    async with aiosqlite.connect('/data/users.db') as db:
        cursor = await db.execute('SELECT * FROM chat_verifications')
        rows = await cursor.fetchall()
        for row in rows:
            print(f'Chat {row[0]} verified by user {row[1]} ({row[3]}) at {row[2]}')

asyncio.run(check())
"
```

## Обратная совместимость

- ✅ Все существующие пользователи в ЛС продолжают работать как раньше
- ✅ Таблица `users` не изменялась
- ✅ Все миграции применяются автоматически при запуске
- ✅ Если `REQUIRED_CHANNELS` не указаны, проверка подписки не выполняется

## Технические детали

### Middleware порядок проверки:
1. Пропускаем ADMIN_CHAT
2. Пропускаем если нет REQUIRED_CHANNELS
3. Пропускаем команды /start и /help
4. Определяем тип чата (ЛС или группа)
5. Для ЛС → проверяем `subscription_verified` пользователя
6. Для чатов → проверяем `chat_verifications` чата

### Фоновая проверка подписок:
- Проверяет личных пользователей (ID > 0) каждые 30 минут
- Проверяет верифицированные чаты - проверяет подписку пользователя-верификатора
- Если верификатор отписался - удаляет верификацию чата

